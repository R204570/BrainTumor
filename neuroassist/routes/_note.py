"""
Add the index route to the session blueprint.
This is appended to routes/session.py conceptually but lives here
so the blueprint registration in app.py stays clean.
"""
# This file is empty — the index route is in routes/session.py
# See the @session_bp.route("/") below:
